import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Selecting the root id and rendering the App component in it
ReactDOM.render(
    <App />,
    document.querySelector('#root')
);