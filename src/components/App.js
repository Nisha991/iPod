import React from 'react';
import Ipod from "./iPod";

// Places the Ipod component in a div container and returns it

const App = () => {
    return (
        <div className="container">
            <Ipod/>
        </div>
        );
};

export default App;